# triage-agent.env.example
# Copy to triage-agent.env (or .env) and fill in values for your environment.
# For Kubernetes: project non-secret values into a ConfigMap, secrets into a Secret.

# --- Memgraph (Graph DB) ---
MEMGRAPH_HOST=memgraph         # Service name in same pod/network (sidecar: localhost)
MEMGRAPH_PORT=7687

# --- 5G Core Kubernetes Namespace ---
CORE_NAMESPACE=5g-core         # K8s namespace where AMF/SMF/UPF/etc. pods run

# --- MCP Data Sources ---
PROMETHEUS_URL=http://prometheus:9090
LOKI_URL=http://loki:3100
MCP_TIMEOUT=3.0

# --- LLM Backend ---
LLM_PROVIDER=local             # openai | anthropic | local
LLM_MODEL=qwen3-4b-instruct-2507.Q4_K_M.gguf
LLM_API_KEY=                   # Required for openai/anthropic; leave blank for local
LLM_BASE_URL=http://qwen3-4b.ml-serving.svc.cluster.local/v1   # Used when LLM_PROVIDER=local
LLM_TIMEOUT=300                # local models are slow (~3 tok/s); 300s covers worst-case RCA responses

# --- Observability ---
LANGSMITH_PROJECT=5g-triage-agent
LANGSMITH_API_KEY=             # Optional; leave blank to disable LangSmith tracing
